set fish_greeting ""

set -gx TERM xterm-256color
set -gx EDITOR lvim
set PATH ~/.local/bin $PATH

# Alias
alias la="exa -al --colour=always --icons --group-directories-first"
alias ll="exa -l --colour=always --icons --group-directories-first"
